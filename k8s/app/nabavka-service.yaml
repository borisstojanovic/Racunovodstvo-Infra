apiVersion: apps/v1
kind: Deployment
metadata:
  name: nabavka-service
  namespace: racunovodstvo-dev
  labels:
    app: nabavka-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nabavka-service
  template:
    metadata:
      labels:
        app: nabavka-service
    spec:
      containers:
        - image: ghcr.io/raf-si-2021/racunovodstvo-nabavka:latest
          name: racunovodstvo-nabavka-service
          imagePullPolicy: Always
          resources: 
            limits:
              cpu: "1"
              memory: 1Gi
          ports:
            - name: nabavka-service
              containerPort: 8084
          env:
            - name: "SPRING_PROFILES_ACTIVE"
              value: "prod"
            - name: "MYSQL_MASTER_HOST"
              - name: "MYSQL_MASTER_HOST"
              value: "jdbc:mysql://database-primary-0.database-primary-headless.racunovodstvo-dev:3306/nabavka?serverTimezone=UTC"
            - name: "MYSQL_SLAVE_HOST"
              value: "jdbc:mysql://database-secondary-0.database-secondary-headless.racunovodstvo-dev:3306/nabavka?serverTimezone=UTC"
            - name: "MYSQL_MASTER_ROOT_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: mysql-root-password
            - name: "MYSQL_SLAVE_ROOT_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: mysql-root-password
            - name: "REGISTRY_HOST"
              value: "eureka"
---
apiVersion: v1
kind: Service
metadata:
  name: nabavka-service
  namespace: racunovodstvo-dev
  labels:
    app: nabavka-service
spec:
  selector:
    app: nabavka-service
  ports:
    - port: 8084
      protocol: TCP
      targetPort: nabavka-service
