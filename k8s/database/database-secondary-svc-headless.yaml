apiVersion: v1
kind: Service
metadata:
  name: database-secondary-headless
  namespace: racunovodstvo-dev
  labels:
    app: database-secondary
spec:
  clusterIP: None
  ports:
    - name: database-sec
      port: 3307
  selector:
    app: database-secondary
